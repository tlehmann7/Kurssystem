editcourses - most of it done

createcourse: check if date is valid

responsive

redirect after logout

wallpaper constant width & height, fixed

logs --> LogID, IP, username, timestamp, action

CREATE TABLE IF NOT EXISTS logs(ID UNSIGNED BIGINT AUTO_INCREMENT, IP varchar(15), userID mediumint, action text);
